server:
  port: 9091
  
eureka:
  client:
    service-url: 
      defaultZon: http://localhost:8761/eureka/
      
spring:
  application:
    name: RESTAURANTLISTING-SERVICE
  datasource:
    url: jdbc:postgresql://localhost:5432/restaurantdb
    password: root
    username: postgres
    driver-class-name:  org.postgresql.Driver
    
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
         